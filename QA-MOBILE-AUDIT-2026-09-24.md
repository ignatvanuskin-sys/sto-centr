# СТО Центр — mobile-first аудит → фиксы → QA (24.09.2026)

Проект: `C:\ЗА БАБКИ\сто центр\sto-centr` (Next.js 15.5.25 + TS + Tailwind 4)
Стенд: production-сборка `http://localhost:3210`
Метод: работающий сайт в реальном браузере (CDP-эмуляция вьюпортов), замеры по DOM
(rect / computed style / контраст / сеть), полный проход сценария, скриншоты.

Важно: у сайта **нет формы записи и броней** — тип бизнеса этого не требует
(основные действия: звонок и WhatsApp). Поэтому вместо booking-flow проверялись
CTA-звонок/WhatsApp, прайс, галерея, контакты и карта.

---

## 1. Объём проверки

| Область | Значения |
|---|---|
| Мобильные брейкпоинты | 320×568, 360×800, 375×812, 390×844, 393×852, 412×915, 430×932, 768×1024 |
| Desktop | 1024×768, 1280×800, 1440×900, 1920×1080 |
| Сценарий | первый экран → CTA-звонок → услуги (8 карточек + весь прайс 93 позиции) → о сервисе → отзывы → галерея/лайтбокс → контакты → карта → маршрут → WhatsApp |
| Технические | `tsc --noEmit`, `eslint`, `next build`, консоль, сеть, overflow, тач-цели, контраст, форматы/вес изображений, SEO, доступность |

---

## 2. Найдено и исправлено

### P1 — фото в лайтбоксе и главные CTA

| # | Проблема | Причина | Фикс | Проверка |
|---|---|---|---|---|
| 1 | **Фото в лайтбоксе не появлялось** (чёрная рамка). На мобильном при клике запрашивались варианты `w=1920` и `w=3840` на кадр шириной 390px; запросы не успевали завершиться (`complete:false`, `naturalWidth:0`) | `sizes="100vw"` у картинки лайтбокса при box ≈ 896px и DPR телефона | `sizes="(max-width:640px) 100vw, (max-width:1024px) 92vw, 896px"`, `quality={78}`, `loading="eager"` | Фото грузится (`naturalWidth=390`, `complete`), запросы `w=640` (30–55 КБ), картинок >1280px на мобильном **нет** |
| 2 | **CTA ниже 44px:** нижняя панель «Позвонить/Написать» и «Уточнить цену» ×8 — 42px, «Связаться» — 40px | `.btn` без минимальной высоты + короткий паддинг | `.btn { min-height: 2.75rem }` (44px) на все кнопки сайта | Замерено 44px у всех кнопок на 320 и 390 |
| 3 | **Мелкие тап-цели у текстовых ссылок:** телефоны и WhatsApp в контактах (19px), «Карточка в 2ГИС», «Политика конфиденциальности» (20px), «Все фото в 2ГИС» (20px), ссылка-рейтинг в hero (19px), «Открыть в 2ГИС» у адреса (20px) | обычные inline-ссылки | Всем проставлен `inline-flex min-h-[44px] items-center` | На 390 и 320: интерактивных элементов <44×44 — **0** |

### P2 — мобильная вёрстка и производительность

| # | Проблема | Фикс | Проверка |
|---|---|---|---|
| 4 | Крестик лайтбокса 40×40 | `size-11` (44×44) | 44×44 |
| 5 | Все 7 фото галереи грузились по мере прокрутки, но первое — `eager` (галерея далеко ниже фолда) | все `loading="lazy"` | В первой загрузке 2 картинки (hero + первое превью), остальные — по появлении |
| 6 | Нижняя панель перекрывала раскрытую «Политику конфиденциальности» на 320×568 (сообщалось 11px) | Запас снизу страницы увеличен до `calc(7rem + env(safe-area-inset-bottom))`, у текста политики добавлен `pb-2` | Политика −159px, © −83px от верха панели — перекрытия нет |
| 7 | Hero на 320×568 был 609px (1.07 экрана) | `py-14 → py-12`; кадрирование фото `object-[50%_45%]` | 617px при 568 (CTA видна без скролла: 389–435px), фото без критичной обрезки |
| 8 | Клик по свободному полю вокруг кадра в лайтбоксе не закрывал просмотр | обработчик на overlay (`event.target === currentTarget`) | Клик по паддингу закрывает, фото при этом загружено |

### P3 — доступность и контраст

| # | Проблема | Было | Стало |
|---|---|---|---|
| 9 | **Оранжевые надзаголовки на светлом фоне** («УСЛУГИ И ЦЕНЫ», «О СЕРВИСЕ», «ФОТО») и оранжевые иконки в карточках «О сервисе» | 2,96–3,20:1 (FAIL) | введён токен `--color-brand-ink: #c2410c` → **4,79–5,18:1** |
| 10 | Подзаголовок в шапке `text-white/45` (11px) на тёмном | 4,16:1 | `text-white/60` → ~19:1 |
| 11 | Мелкий текст футера: © и текст политики | 4,16 / 5,68 | `text-white/60` / `/70` → 6,91 / 9,00 |
| 12 | Логотип-ссылка и телефон в шапке — 36px и 20px | — | `min-h-[44px]` |

Ложные срабатывания первого прохода (проверены отдельно, правок не требуют):
`prefers-reduced-motion` и `:focus-visible` **есть** в собранном CSS; контраст имени автора
отзыва — реально 19,21 (артефакт парсинга `oklab()`), а не 3,01.

---

## 3. Итог приёмки

| Проверка | Результат |
|---|---|
| Горизонтальное переполнение 320→1920 (12 ширин) | **0 элементов** |
| Консоль | **0 ошибок, 0 предупреждений**, JS-исключений нет |
| Сеть | неудачных запросов нет; галерея `w=384/640`, лайтбокс `w=640`; >1280px на мобильном нет; первая загрузка ≈210 КБ (24 запроса) |
| Тап-цели | все интерактивные элементы ≥44×44 на 320 и 390 (**0 исключений**) |
| Контраст текста | всё проверенное ≥4,79:1 |
| Hero 320×568 | 617px, CTA видна без скролла, h1 не обрезан |
| Якорные ссылки | заголовки секций не уходят под sticky-шапку (`scroll-mt-20`, запас 80px) |
| Галерея | свайп со snap на мобильном, сетка 3 колонки на desktop, лайтбокс: стрелки, Escape, крестик, клик по фону, листание по кругу |
| Прайс | 8 карточек + раскрытие 93 позиций (58 + 35), без переполнения и «прыжков» |
| Ссылки | tel ×3, WhatsApp ×2, 2ГИС ×4, маршрут — все корректны |
| Desktop 1280/1440/1920 | регрессий нет: 4 карточки услуг в ряд, галерея 3 колонки, контакты 2 колонки + карта |
| SEO | title, description, canonical, OG, JSON-LD `AutoRepair`, robots, sitemap — на месте |
| `tsc --noEmit` / `eslint` / `next build` | чисто / чисто / успешно (`/` 13,2 КБ, First Load JS 116 КБ) |

Скриншоты: 320 hero, 390 hero, 390 услуги, 390 лайтбокс, 390 контакты, 768 hero, 1440 hero,
1440 контакты (`preview-audit/`).

---

## 4. Изменённые файлы

```
app/globals.css                 токен brand-ink, min-height 44px для .btn
app/page.tsx                    запас снизу страницы под фиксированную панель
components/Hero.tsx             py-12, кадрирование фото, тап-цель ссылки-рейтинга
components/Header.tsx           min-h 44 у логотипа и телефона, контраст подзаголовка
components/Services.tsx         цвет надзаголовка (brand-ink)
components/About.tsx            цвет надзаголовка и иконок (brand-ink)
components/Gallery.tsx          лайтбокс: sizes/quality/eager, крестик 44px, клик по фону,
                                lazy для всех фото, тап-цель ссылки, цвет надзаголовка
components/Contacts.tsx         тап-цели телефонов, WhatsApp и ссылки «Открыть в 2ГИС»
components/Footer.tsx           тап-цели ссылок и summary, контраст мелкого текста
```

---

## 5. Осталось / на решение владельца

1. **Hero на 320×568 — 617px** (чуть выше одного экрана). Главная CTA видна без скролла,
   поэтому оставлено как есть: чтобы ужать до 568px, пришлось бы убирать строку
   с адресом/рейтингом или уменьшать заголовок до нечитаемого.
2. **Логотип в шапке** теперь 44px по высоте — на очень узких экранах шапка стала чуть
   «воздушнее» (64px). Если хочется строже — можно вернуть 36px, но тогда это снова
   мелкая мишень.
3. **Фото из 2ГИС с водяным знаком 2ГИС.** Юридически это открытая карточка организации;
   если владелец хочет «чистый» сайт — нужны собственные фото сервиса.
4. **Прайс-лист в 2ГИС обновлён 25.06.2020** — на сайте есть пометка «уточняйте по телефону»;
   стоит получить актуальные цены у владельца.
5. **Домен** для деплоя (`NEXT_PUBLIC_SITE_URL`) — иначе OG-картинки останутся на localhost.
6. Инструмент скриншотов в среде периодически отдавал устаревший кадр (окно Chrome свёрнуто) —
   все ключевые выводы сделаны по DOM-замерам, скриншоты приложены как иллюстрации.
